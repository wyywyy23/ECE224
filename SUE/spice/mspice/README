This "mspice" directory contains all of the spice primitives.  It is
different from the "spice" directory in that it converts transistor
widths into microns (thus "m" for micron spice).  Thus if you want to
specify W=6um, just set W=6 and use the mspice directory.

